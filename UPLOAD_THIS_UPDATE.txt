LinaHub 17.4.2 — Universal Image Upload Fix

Upload all files in this ZIP over the existing LinaHub repository, preserving the folders.

Changed only the image upload/save system:
- One shared module: core/media.js
- Automatic resize and compression
- WebP output with JPEG fallback
- Existing IndexedDB database and store retained
- Save verification before success is shown
- Clear errors without silently removing the previous image
- Shared handling for Home tile images, banners, Girls Tank, Boys Tank and medication photo processing

No design or UI changes were made.
