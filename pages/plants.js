function plantStatus(p){if(!p.lastWatered)return {text:"Needs attention",className:"attention",icon:"🔔"};const days=Math.floor((new Date(today()+"T12:00:00")-new Date(p.lastWatered+"T12:00:00"))/86400000);return days>=5?{text:"Needs attention",className:"attention",icon:"🔔"}:{text:"All good",className:"good",icon:"🌿"}}
function PlantsPage(){const attention=data.plants.filter(p=>plantStatus(p).className==="attention").length,photos=data.plants.filter(p=>p.photo).length;return shell(`${head("Plants","Care for your enchanted garden")}<section class="premium-hero plant-premium-hero"><div><span class="section-kicker">🌿 Your collection</span><h1>Plants</h1><p>Keep watering, photos and care notes together.</p></div><div class="hero-orb">🌸</div></section><div class="plant-search"><span>⌕</span><input id="plantSearch" placeholder="Search plants…"><span>☷</span></div><section class="plant-metrics"><div><span>🌿</span><strong>${data.plants.length}</strong><small>Plants</small></div><div><span>🔔</span><strong>${attention}</strong><small>Need attention</small></div><div><span>📷</span><strong>${photos}</strong><small>Photos</small></div><div><span>✓</span><strong>${data.plants.length-attention}</strong><small>All good</small></div></section><div class="section-row"><div><span class="section-kicker">Collection</span><h2>Your plants</h2></div></div><section class="premium-list" id="plantList">${data.plants.map(p=>{const s=plantStatus(p);return `<button type="button" class="premium-list-row" data-route="plant" data-route-id="${esc(p.id)}" data-plant-name="${esc(p.name.toLowerCase())}"><div class="list-thumb">${p.photo?`<img src="${p.photo}" alt="${esc(p.name)}">`:`<span>${p.emoji}</span>`}</div><div><strong>${esc(p.name)}</strong><small>${p.lastWatered?`Last watered ${formatDate(p.lastWatered)}`:"No watering logged"}</small><em class="status-chip ${s.className}">${s.icon} ${s.text}</em></div><b>›</b></button>`}).join("")}</section>`,"plants")}
function PlantProfilePage(){const p=data.plants.find(x=>x.id===routeId);if(!p)return PlantsPage();const history=[...(p.history||[])].sort().reverse(),status=plantStatus(p);return shell(`${head(p.name,"Plant profile","plants")}<section class="plant-cover">${p.photo?`<img src="${p.photo}" alt="${esc(p.name)}">`:`<div>${p.emoji}</div>`}<label class="photo-fab">📷<input id="plantPhoto" type="file" accept="image/*" hidden></label></section><section class="plant-title-panel"><div><span class="section-kicker">🌿 Plant profile</span><h1>${esc(p.name)}</h1><em class="status-chip ${status.className}">${status.icon} ${status.text}</em></div></section><div class="profile-tabs"><button class="active">Overview</button><button>Care</button><button>History</button><button>Notes</button></div><section class="care-summary"><div><small>Last watered</small><strong>${p.lastWatered?formatDate(p.lastWatered):"Not yet"}</strong></div><div><small>Waterings logged</small><strong>${history.length}</strong></div></section><section class="card clean-card"><div class="section-row"><div><span class="section-kicker">Care</span><h2>Quick actions</h2></div></div><button type="button" class="primary" id="waterPlant">💧 Watered today</button></section><section class="card clean-card"><span class="section-kicker">Notes</span><h2>Care notes</h2><textarea class="field plant-notes" rows="5" placeholder="Growth, placement, care reminders…">${esc(p.notes)}</textarea><button type="button" class="secondary" id="savePlantNotes">Save notes</button></section><section class="card clean-card"><span class="section-kicker">History</span><h2>Watering history</h2><div class="history-list">${history.length?history.map(d=>`<div class="history-row"><span>💧</span><span>${esc(formatDate(d))}</span></div>`).join(""):`<p>No watering history yet.</p>`}</div></section>`,"plants")}
function bindPlants(){document.querySelector("#plantSearch")?.addEventListener("input",e=>{const q=e.target.value.toLowerCase();document.querySelectorAll("[data-plant-name]").forEach(row=>row.hidden=!row.dataset.plantName.includes(q))});document.querySelector("#waterPlant")?.addEventListener("click",()=>{const p=data.plants.find(x=>x.id===routeId);if(!p)return;const d=today();p.lastWatered=d;if(!p.history.includes(d))p.history.push(d);saveData();toast(`${p.name} watered 💧`);render()});document.querySelector("#savePlantNotes")?.addEventListener("click",()=>{const p=data.plants.find(x=>x.id===routeId);if(!p)return;p.notes=document.querySelector(".plant-notes").value;saveData();toast("Plant notes saved 🌿")});document.querySelector("#plantPhoto")?.addEventListener("change",e=>{const file=e.target.files?.[0];if(!file)return;const reader=new FileReader();reader.onload=()=>{const p=data.plants.find(x=>x.id===routeId);if(!p)return;p.photo=reader.result;saveData();toast("Plant photo saved 📷");render()};reader.readAsDataURL(file)})}
